{
  "name": "wastedjs",
  "version": "0.0.1",
  "description": "WastedJS",
  "main": "index.js",
  "scripts": {
    "client": "cd client && yarn start",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\"",
    "start": "node server.js",
    "heroku-postbuild": "cd client && yarn && yarn run build"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "concurrently": "^4.0.1",
    "nodemon": "^2.0.4",
    "prettier": "2.0.5"
  },
  "type": "module",
  "license": "MIT"
}
